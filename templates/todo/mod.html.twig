{% extends 'base.html.twig' %} {% form_theme formTache 'bootstrap_4_layout.html.twig' %} {% block body %}

<h1>Modifier la tâche</h1>

{{ form_start(formTache) }} {{ form_row(formTache.title) }} {{ form_row(formTache.content) }}

<button type="submit" class="btn btn-success">Modifier la tache</button> {{ form_end(formTache) }} {% endblock %}